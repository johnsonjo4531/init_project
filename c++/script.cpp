#include <bits/stdc++.h>
using namespace std;
// Remember \n works faster than endl as it doesn't flush.

int main () {
  ios::sync_with_stdio(0);
  cin.tie(0);
  // code goes here
  cout << "foo\n";
  while (cin >> x) {
    // code
  }
  return 0;
}
